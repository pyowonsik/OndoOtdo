<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 배경 색상 -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- 테두리 색상 -->
    <corners android:radius="16dp" /> <!-- 둥근 모서리 (radius 값으로 곡률 조정) -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /> <!-- 안쪽 여백 -->
</shape>
